# --- agent / control plane -------------------------------------------------
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
pydantic>=2.6
requests>=2.31

# --- eval harness ------------------------------------------------------------
matplotlib>=3.8

# --- optional convenience -----------------------------------------------------
python-dotenv>=1.0

# --- NOT pip-installable in the usual sense, listed here for reference only --
# ryu           -> install from the OS/venv per README.md (needs an eventlet-
#                  compatible Python; Ryu is unmaintained upstream and picky
#                  about newer setuptools/eventlet versions)
# mininet       -> install via the official Mininet install script or apt
#                  (`sudo apt install mininet`), not pip — it needs Open
#                  vSwitch and root privileges to create network namespaces.
# iperf3        -> `sudo apt install iperf3`
